$kind: http-request
url: "{{base_url}}/api/users/{{user_id}}"
method: PUT
headers:
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "role_id": 3,
        "is_active": false
    }
order: 5000
